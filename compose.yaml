services:
  mcp-hub:
    build:
      context: .
    container_name: flamoris-mcp-hub
    restart: unless-stopped
    environment:
      FLAMORIS_MCP_HUB_CONFIG_DIR: /app/config/mcps
      FLAMORIS_MCP_HUB_ENV_FILE: /app/.env
      FLAMORIS_MCP_HUB_ALLOWED_HOSTS: ${FLAMORIS_MCP_HUB_ALLOWED_HOSTS:-}
      FLAMORIS_MCP_HUB_ALLOWED_ORIGINS: ${FLAMORIS_MCP_HUB_ALLOWED_ORIGINS:-}
    volumes:
      - ./config/mcps:/app/config/mcps:ro
      - ./.env:/app/.env:ro
    ports:
      - "${FLAMORIS_MCP_HUB_BIND:-127.0.0.1}:${FLAMORIS_MCP_HUB_PORT:-8765}:8765"
